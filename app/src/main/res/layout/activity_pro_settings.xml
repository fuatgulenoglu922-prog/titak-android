<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg_cyber"
    android:padding="20dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="PRO SES DEĞİŞTİRİCİ"
        android:textColor="@color/neon_cyan"
        android:textSize="22sp"
        android:textStyle="bold"
        android:layout_marginBottom="24dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Arama sırasında kullanılacak ses efektini seçin:"
        android:textColor="@color/text_mid"
        android:textSize="14sp"
        android:layout_marginBottom="16dp" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">
        
        <LinearLayout
            android:id="@+id/container_effects"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">
            <!-- Programmatically added buttons or static list -->
        </LinearLayout>
    </ScrollView>

    <Button
        android:id="@+id/btn_save_effect"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:text="KAYDET VE DÖN"
        android:backgroundTint="@color/accent_green"
        android:textColor="@color/black"
        android:textStyle="bold"
        android:layout_marginTop="16dp"/>
</LinearLayout>
